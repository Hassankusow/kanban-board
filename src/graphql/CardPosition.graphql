mutation UpdateCardPosition(
  $id: uuid!
  $column_id: uuid!
  $order_index: float8!
) {
  update_cards_by_pk(
    pk_columns: { id: $id }
    _set: { column_id: $column_id, order_index: $order_index }
  ) {
    id
    column_id
    order_index
  }
}
